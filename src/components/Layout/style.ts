import Styled from 'styled-components';

export const Container = Styled.div``;

export const Content = Styled.div``;

export const InternetConnectionContainer = Styled.div`
    width: 100%;
    text-align: center;

    background: var(--dark-green);
    color: #fff;
`;
